---
import BaseLayout from '../../layouts/BaseLayout.astro';
import AdSlot from '../AdSlot.astro';
import CarTaxCalculator from '../CarTaxCalculator.astro';
import { t } from '../../i18n/ui';
import { localizePath } from '../../i18n/config';

const lang = 'ko';
const tr = t(lang);
const c = tr.cartax;
const related = [
  { href: localizePath(lang, '/vat'), label: tr.nav.vat },
  { href: localizePath(lang, '/income-tax'), label: tr.nav.incomeTax },
  { href: localizePath(lang, '/jeonse'), label: tr.jeonse.title },
];
---

<BaseLayout lang={lang} path="/car-tax" title={`${c.title} | FinCalc`} description={c.description}>
  <section class="page-head container">
    <h1>{c.title}</h1>
    <p class="page-head__desc">{c.description}</p>
  </section>

  <div class="container layout-2col">
    <div class="layout-main">
      <CarTaxCalculator lang={lang} />
    </div>
    <aside class="layout-side">
      <AdSlot variant="rectangle" />
    </aside>
  </div>

  <section class="container section">
    <p class="related">
      {related.map((r, i) => (<>
        {i > 0 && ' · '}
        <a href={r.href}>{r.label}</a>
      </>))}
    </p>
  </section>

  <div class="container">
    <AdSlot variant="leaderboard" />
  </div>
</BaseLayout>
